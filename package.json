{
  "name": "lms-app",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "concurrently \"npm run backend\" \"npm run frontend\"",
    "backend": "cd backend && npm run dev",
    "frontend": "cd frontend && npx expo start",
    "start": "start cmd /k \"cd backend && npm start\" && start cmd /k \"cd frontend && npx expo start\""
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "supabase": "^2.34.3"
  }
}
